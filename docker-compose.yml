services:
  unilabos-ui:
    image: ${UNILABOS_IMAGE:-registry-1.docker.io/styxhuang/unilabos:latest}
    container_name: unilabos-ui
    platform: linux/amd64
    ports:
      - "${UNILABOS_PORT:-50003}:8000"
    restart: unless-stopped
    environment:
      PYTHONUNBUFFERED: "1"
